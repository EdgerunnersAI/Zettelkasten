"""KG Intelligence Layer (M1-M6) tests package.

8.0-H7: this __init__.py previously held test code mirroring test_nl_query.py
that imported the now-deleted ``website.features.kg_features.nl_query``.
Cleared to a normal package marker; the actual nl_query tests live in
test_nl_query.py (skip-marked at module level for the same reason).
"""
